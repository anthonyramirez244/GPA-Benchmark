# PGO-skill Ledger — blackscholes

Append-only log of optimization attempts. Do not edit or delete past entries — only append.
Before proposing a new optimization, check here first for prior attempts on the same kernel + optimizer.

---

### 2026-08-11T06:17:43.680837+00:00 — blackscholes
- Problem size: (no args)
- Correctness: PASS (stdout contained pass marker)
- End-to-end: 0.6569s (σ=0.0129, n=3) -> 0.6591s (σ=0.0317, n=3) (0.997x) [NOT SIGNIFICANT, within 2σ noise]
- Local kernel timing (nsys): unavailable (nsys captured no GPU kernel activity records on this platform (known limitation on some WSL2/driver combinations) -- end-to-end timing is still valid, local kernel timing is not)
- Self-reported kernel timing BlackScholesGPU: 191088ns (σ=992, n=3) -> 197242ns (σ=816, n=3) (0.969x) [clears 2σ]
- Change: BlackScholes_kernel.cuh — in `BlackScholesGPU`, hoisted the three `float2` global loads (`d_StockPrice[opt]`, `d_OptionStrike[opt]`, `d_OptionYears[opt]`) into named local temporaries before both `BlackScholesBodyGPU` calls, addressing `GPUCodeReorderOptimizer`'s top finding (ratio 63.851%, estimated 1.261x, `GINS:LAT_GMEM_GMEM` concentrated at lines 65/66/102: the first call's math depends immediately on a load that just issued, leaving no independent work to hide the ~400-cycle global memory latency behind). Cross-benchmark prior for this optimizer was mixed (3 KEPT/4 REVERTED, `anyStallReason`; no prior data for this specific stall reason) but still the highest-impact candidate by a wide margin (impactScore 0.167 vs. next-best 0.017). Note: this finding also tripped the new `possiblyAlgorithmic` heuristic (94.2% memory-latency concentration, 63.9% of kernel time), which this session judged a likely false positive before attempting the fix — `d_StockPrice`/`d_OptionStrike`/`d_OptionYears` are already fully coalesced, contiguous `float2` loads (unlike XSBench's scattered energy-indexed gather that motivated the heuristic), so this reads as an instruction-scheduling/ILP problem, not a data-layout problem — logged here as a live test of that judgment.
- Decision: REVERTED — significant regression. Correctness held, end-to-end was flat/not-significant (0.997x), but self-reported kernel timing clears the 2σ gate at 0.969x (191,088ns → 197,242ns, ~3.1% slower, real given ~1% relative noise on both sides) — the governing metric per SKILL.md step 8's precedence (self-reported > nsys > end-to-end), and this benchmark's self-reported timer is a clean, isolated, nsys-independent per-kernel signal (not a combined/impure metric like gaussian's or lud's). Most likely explanation: the explicit `float2` temporaries force all three loads' registers to stay live simultaneously across both `BlackScholesBodyGPU` calls, increasing register pressure/reducing occupancy, when the compiler's own scheduler (given `__restrict__` pointers to prove no aliasing) was likely already free to reorder these loads as needed without paying that extra-live-register cost — a case where GPA's instruction-level blame analysis correctly identified a real stall but the human-legible "textbook" fix for it backfired against actual measurement, consistent with this optimizer's mixed 3/7 cross-benchmark success rate. Also a partial answer to the false-positive question raised above: whether or not `possiblyAlgorithmic` was right to flag this finding, no instruction-level `GPUCodeReorderOptimizer` fix helped here either way, which is at least consistent with (though not proof of) the flag's suspicion that something other than ordinary instruction scheduling is the real limiter.
